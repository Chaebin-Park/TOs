<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="deu.cse.tos">

    <uses-permission android:name="android.permission.INTERNET" />

    <queries>
        <package android:name="com.kakao.talk" />
    </queries>

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/TosTheme">

        <activity android:name=".LoginActivity"/>
        <activity android:name=".MainActivity"/>
        <activity android:name=".VideoActivity"/>
        <activity android:name=".BrushListActivity" />
        <activity android:name=".AddBrushListActivity"/>
        <activity android:name=".TimerActivity"/>
        <activity android:name=".ShowQnAActivity"/>
        <activity android:name=".ModeActivity"/>
        <activity android:name=".SelfCheckActivity"/>
        <activity android:name=".CheckResultActivity"/>
        <activity android:name=".NotLoginActivity"/>
        <activity android:name=".IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>